/**
 * SuiteScript API 2.0 Util.
 *
 * @NApiVersion 2.x
 * @NModuleScope SameAccount
 * @author Volnei Ferreira
 */
define([],
function () {

    /**
     * @namespace
     */
    var UtilNs2 = {
        // no libraries so far
    };

    return UtilNs2;
});
